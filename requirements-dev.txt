# Development & CI-only dependencies.
# Install with:  pip install -r requirements-dev.txt
# Runtime deps are in requirements.txt; this file extends it.

-r requirements.txt

# Lint / format — unified tool replacing black+pylint+isort
ruff==0.7.3

# Test infrastructure
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-alembic==0.11.1        # used by tests/test_migrations.py
httpx==0.27.2                 # FastAPI TestClient underlying transport

# Security scanning (CI)
safety==3.2.11
